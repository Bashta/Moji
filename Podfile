# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'Moji' do
  use_frameworks!
  
  pod 'Firebase/Core'
  pod 'Fabric'
  pod 'Crashlytics'

  target 'MojiTests' do
    inherit! :search_paths
  end

  target 'MojiUITests' do
    inherit! :search_paths
  end

end
